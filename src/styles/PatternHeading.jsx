import styled from 'styled-components';

const PatternHeading = styled.span`
  color: ${({ theme }) => theme.colors.secondary};
  font-size: 20px;
  font-weight: 600;
`;

export default PatternHeading;
